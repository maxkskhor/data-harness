[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dataact"
version = "0.1.1"
description = "Data agent with Python-native tools (no bash)"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Max Khor" },
]
requires-python = ">=3.10"
dependencies = [
    "anthropic",
    "loguru",
    "pandas",
    "numpy",
]

[project.optional-dependencies]
dev = [
    "openai",
    "pytest",
    "pytest-mock",
    "python-dotenv",
]
openai = [
    "openai",
]

[project.urls]
Homepage = "https://github.com/maxkskhor/dataact"
Repository = "https://github.com/maxkskhor/dataact"
Issues = "https://github.com/maxkskhor/dataact/issues"

[tool.hatch.build.targets.wheel]
packages = ["dataact"]

[tool.uv]
dev-dependencies = [
    "openai",
    "pytest",
    "pytest-mock",
    "python-dotenv",
    "ruff",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
markers = [
    "live: smoke tests that call real provider APIs (deselect with -m 'not live')",
]
